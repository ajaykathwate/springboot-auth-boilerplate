# =============================================
# Spring Boot Auth Boilerplate - Environment Variables
# =============================================
# Copy this file to .env and fill in your values
# cp .env.example .env
# =============================================

# =====================
# Server Configuration
# =====================
APP_SERVER_PORT=8080

# =====================
# Database (PostgreSQL)
# =====================
# For local development with Docker:
DATASOURCE_HOST=localhost
DATASOURCE_PORT=5432
DATASOURCE_NAME=authdb
DATASOURCE_USERNAME=postgres
DATASOURCE_PASSWORD=postgres

# For Docker container (use service name):
# DATASOURCE_HOST=auth-postgres

# =====================
# Redis
# =====================
# For local development with Docker:
REDIS_HOST=localhost
REDIS_PORT=6379

# For Docker container (use service name):
# REDIS_HOST=auth-redis

# =====================
# RabbitMQ
# =====================
# For local development with Docker:
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# For Docker container (use service name):
# RABBITMQ_HOST=auth-rabbitmq

# =====================
# JWT Secrets (REQUIRED)
# =====================
# Generate secure keys using: openssl rand -base64 32
# IMPORTANT: Use different keys for access and refresh tokens
APP_JWT_SECRET=your-256-bit-secret-key-here-generate-with-openssl
APP_JWT_REFRESH_SECRET=your-different-256-bit-secret-key-here

# =====================
# Google OAuth (REQUIRED for Google login)
# =====================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_AUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com

# =====================
# FrontEnd Configuration
# =====================
FRONTEND_BASE_URL=http://localhost:3000

# =====================
# Email/SMTP Configuration
# =====================
# Note: Configure directly in application.yaml
# For Gmail, use App Password (not regular password)
# Get App Password from: https://myaccount.google.com/apppasswords

# =====================
# Twilio (OPTIONAL - for SMS/WhatsApp)
# =====================
# Sign up at: https://www.twilio.com/
TWILIO_ENABLED=false
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_WHATSAPP_NUMBER=

# =====================
# Firebase (OPTIONAL - for Push Notifications)
# =====================
# Get service account from: Firebase Console > Project Settings > Service Accounts
FIREBASE_ENABLED=false
FIREBASE_SERVICE_ACCOUNT_PATH=

# =====================
# Quick Setup Guide
# =====================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secrets: openssl rand -base64 32 (run twice for both)
# 3. Get Google Client ID from Google Cloud Console
# 4. Get Gmail App Password (not regular password)
# 5. Start services: docker-compose up -d
# 6. Run app: ./mvnw spring-boot:run
